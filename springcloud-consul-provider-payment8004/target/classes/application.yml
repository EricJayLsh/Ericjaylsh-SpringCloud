
server:
  port: 8004

# consul 注册配置如下：

spring:
  application:
    name: springcloud-provider-payment
  cloud:
    consul:
      host: ericjaylsh.dev.xyz
      port: 8500
      discovery:
        service-name: ${spring.application.name}